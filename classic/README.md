# classic — Pure classical baselines

**Rule:** No Spiral code paths. No residual fields. No SRT. No theory-core.

Standard scikit-learn estimators + OpenML datasets. spiral_informed = False.

```bash
cd classic && python evaluate.py
```
